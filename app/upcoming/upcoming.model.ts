export class Upcomings {
  summary: string;
  // htmlLink: string;
  // startDate: string;
  // startTime: string;
  // endDate: string;
  // endTime: string;
  constructor(sum:string){
    this.summary = sum;
  }
}